# 服装行业 AI 趋势分析 Agent —— 依赖清单
# 安装：pip install -r requirements.txt
# 生成于 2026-08-15，基于 venv 实测版本

openai==2.52.0
pandas==3.0.5
streamlit==1.60.0
matplotlib==3.11.1
pydantic==2.13.4
python-dotenv==1.2.2
numpy==2.5.1

# RAG 相关（v8/v9）
chromadb==1.5.9
fastembed==0.8.0

# 联网搜索（DuckDuckGo 免费，无需 key）
ddgs==9.14.4

# 测试（开发用）
pytest==9.1.1
